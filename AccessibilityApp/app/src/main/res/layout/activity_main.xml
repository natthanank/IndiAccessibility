<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="This app uses an Accessibility Service to provide a floating menu with system controls. Please enable both the Accessibility Service and the Overlay Permission below.\n\nThe service will automatically disable itself when you open certain apps (e.g., Settings) and will need to be manually re-enabled."
        android:layout_marginBottom="24dp"
        android:gravity="center_horizontal" />

    <Button
        android:id="@+id/enable_accessibility_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enable Accessibility Service" />

    <Button
        android:id="@+id/enable_overlay_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enable Overlay Permission"
        android:layout_marginTop="16dp"/>

</LinearLayout>